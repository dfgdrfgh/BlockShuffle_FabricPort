name: Build Fabric versions

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        minecraft:
          - "1.21"
          - "1.21.1"
          - "1.21.2"
          - "1.21.3"
          - "1.21.4"
          - "1.21.5"
          - "1.21.6"
          - "1.21.7"
          - "1.21.8"
          - "1.21.9"
          - "1.21.10"
          - "1.21.11"
          - "26.2"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.minecraft == '26.2' && '25' || '21' }}
      - name: Build Minecraft ${{ matrix.minecraft }}
        run: ./gradlew -PmcVersion=${{ matrix.minecraft }} clean build
      - uses: actions/upload-artifact@v4
        with:
          name: blockshuffle-fabric-${{ matrix.minecraft }}
          path: build/libs/BlockShuffle_FabricPort-1.0.0-fabric.1+mc${{ matrix.minecraft }}.jar
